import React from "react"

import {OpeningHours} from "./components"

const App = (): React.ReactElement => {
  return (
    <div className="App">
      <OpeningHours />
    </div>
  )
}

export default App
